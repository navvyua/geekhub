%h1 Posts

%table
  %thead
    %tr
      %th ID
      %th Author
      %th Title
  %tbody
    - @posts.each do |post|
      %tr
        %td = post.id
        %td = post.user.email
        %td = post.title
        %td = link_to 'Delete', admin_post_path(post), method: :delete